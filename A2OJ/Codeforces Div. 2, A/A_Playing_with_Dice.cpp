#include <bits/stdc++.h>
using namespace std;
int main()
{
    int a, b;
    cin >> a >> b;
    if (a == b)
    {
        cout << 0 << " " << 6 << " " << 0;
    }
    else
    {
        if (a < b)
        {
            if ((a + b) %2 != 0)
            {
                int draw = 0;
                if(a >)
            }
        }
        else
        {
        }
    }
    return 0;
}